---
apiVersion: apps.open-cluster-management.io/v1
kind: Channel
metadata:
  name: openshift-release-dev-images
  namespace: ocp-clusterimagesets
spec:
  type: GitHub
  pathname: https://github.com/open-cluster-management/acm-hive-openshift-releases.git
  # Disable when using a Public repository
  #secretRef:
  #  name: my-github-secret